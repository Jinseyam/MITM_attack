# MITM_attack
mitm.py -> A monster-in-the-middle (MITM) attack on a simulated banking network using Python. The goal is to intercept, tamper with, and forward traffic between a victim and a bank server, without the victim detecting my interference.

detector.py ->  a Python script to automatically detect two types of misbehavior inside any PCAP (packet capture) file
